#include <iostream>
using namespace std;

/*int main()
{
	int total = 0;
	int kandidat_a = 0;
	int kandidat_b = 0;
	int kandidat_c = 0;

	cout << "Bitte geben Sie die Gesamtzahl der abgegebenen gueltigen Stimmen ein: ? ";
	cin >> total;
	cout << "Bitte geben Sie die Anzahl Stimmen des ersten Kandidaten ein: ? ";
	cin >> kandidat_a;
	cout << "Bitte geben Sie die Anzahl Stimmen des zweiten Kandidaten ein: ? ";
	cin >> kandidat_b;
	kandidat_c = total - (kandidat_a + kandidat_b);
	cout << "Auf den dritten Kandidaten sind somit " << kandidat_c << " Stimmen entfallen.\n";
	
	cout << "Kandidat 1 erhielt " << (double(kandidat_a) / (total)) * 100 << "% der Stimmen." << endl;
	cout << "Kandidat 2 erhielt " << (double(kandidat_b) / (total)) * 100 << "% der Stimmen." << endl;
	cout << "Kandidat 3 erhielt " << (double(kandidat_c) / (total)) * 100 << "% der Stimmen." << endl;
	
	system("PAUSE");
	return 0;
}*/