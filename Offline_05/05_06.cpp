#include <iostream>
using namespace std;

/*int main()
{
	// array fuer wiederausgabe der eingaben + integer fuer eingabe, max und min wert
	int zahlen[5];
	int max = 0;
	int min = 0;
	int eingabe = 0;

	// for loop zum definieren der min und max werte, so wie erfragen der eingaben
	for (int i = 0; i >= 0 && i < 5; i++)
	{
		cout << "Bitte geben Sie die " << i + 1 << ". Zahl ein: ? ";
		cin >> eingabe;

		zahlen[i] = eingabe;

		if (eingabe > max || i == 0)
		{
			max = eingabe;
		}
		if (eingabe < min || i == 0)
		{
			min = eingabe;
		}
	}
	// for loop zur ausgabe der eingaben
	for (int i = 0; i >= 0 && i < 5; i++)
	{
		cout << "Die " << i + 1 << ". eingegebene Zahl lautete: " << zahlen[i] << endl;
	}
	// ausgabe der min und max werte
	cout << "Die kleinste eingegebene Zahl lautete: " << min << endl;
	cout << "Die groesste eingegebene Zahl lautete: " << max << endl;

	system("PAUSE");
	return 0;
}*/