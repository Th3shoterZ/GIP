#include <iostream>
using namespace std;

/*int main()
{
	// array als speicher der zahlen und ein integer fuer die eingabe
	int speicher[4];
	int eingabe = 0;

	// zwei int werte fuer das zusammenrechnen der gleichen oder ungleichen zahlen am ende des loops
	int summe_gleich = 0;
	int summe_ungleich = 0;

	// for loop im die eingaben dem array zu zuweisen
	for (int i = 0; i >= 0 && i < 4; i++)
	{
		cout << "Bitte geben Sie die " << i + 1 << ". Zahl ein: ? ";
		cin >> eingabe;
		speicher[i] = eingabe;
	}

	// nach vergleichszahl fragen
	cout << "Bitte geben Sie die Vergleichszahl ein: ? ";
	cin >> eingabe;

	// for loop zum bestimmen der gleichheit oder ungleichheit der werte
	for (int i = 0; i >= 0 && i < 4; i++)
	{
		if (speicher[i] == eingabe)
		{
			summe_gleich = summe_gleich + 1;
		}
		else
		{
			summe_ungleich = summe_ungleich + 1;
		}
	}

	// ausgabe der werte berechneten summen
	cout << summe_gleich << " Eingabezahlen waren gleich der Vergleichszahl." << endl;
	cout << summe_ungleich << " Eingabezahlen waren ungleich der Vergleichszahl." << endl;
	
	system("PAUSE");
	return 0;
}*/