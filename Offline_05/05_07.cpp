#include <iostream>
#include <string>
using namespace std;

/*int main()
{
	// string fuer die eingabe
	string mein_string;
	int x = 1;
	int neue_zahl = 0;

	
	cout << "Bitte die Zahl oder das Wort 'ende' eingeben: ? ";
	getline(cin, mein_string);
	
	// if zum entscheiden ob es sich um zahlen oder 'ende' handelt
	if (mein_string == "ende")
	{
		cout << "Das Programm beendet sich jetzt.\n";
	}
	else
	{
		// for loop um die zahlen von char in int umzuwandeln
		for (int i = mein_string.length() - 1; i >= 0 && i < mein_string.length(); i--)
		{

			neue_zahl = neue_zahl + ((int(mein_string.at(i)) - int('0')) * x);
			x = x * 10;
		}
		cout << "Der doppelte Wert betraegt: " << neue_zahl * 2 << endl;
	}


	system("PAUSE");
	return 0;
}*/