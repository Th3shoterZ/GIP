#include <iostream>
#include <string>
using namespace std;

/*int main()
{
	string my_string;
	char suchzeichen = 'a';
	char ersatzzeichen = 'a';

	cout << "Bitte Text eingeben (ggfs. mit Leerzeichen): ? ";
	getline(cin, my_string);
	cout << "Bitte den zu ersetzenden Buchstaben eingeben: ? ";
	cin >> suchzeichen;
	cout << "Bitte den Ersatz-Buchstaben eingeben: ? ";
	cin >> ersatzzeichen;

	for (unsigned int i = 0 ; i >= 0 && i < my_string.length(); i++)
	{
		if(my_string.at(i) == suchzeichen)
		{
			my_string.at(i) = ersatzzeichen;
		}
	}
	cout << "Der Text nach der Ersetzung: " << my_string << endl;


	system("PAUSE");
	return 0;
}*/