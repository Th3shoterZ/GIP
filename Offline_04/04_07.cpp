#include <iostream>
#include <string>
using namespace std;

/*int main()
{
	string my_string;
	string suchzeichen;
	int pos = 0;
	bool gefunden = false;
	

	cout << "Bitte Text eingeben (ggfs. mit Leerzeichen): ? ";
	getline(cin, my_string);
	cout << "Bitte Buchstaben eingeben: ? ";
	getline(cin, suchzeichen);

	for (unsigned int i = 0; i >= 0 && i < my_string.length(); i++)
	{
		if(my_string.at(i) == suchzeichen.at(0))
		{
			pos = i;
			gefunden = true;
		}
	}
	if (gefunden == true)
	{
		cout << "Der Buchstabe " << suchzeichen << " kommt im Text vor, an Position " << pos << ".\n";
	}
	else
	{
		cout << "Der Buchstabe " << suchzeichen << " kommt nicht im Text vor.\n";
	}

	system("PAUSE");
	return 0;
}*/