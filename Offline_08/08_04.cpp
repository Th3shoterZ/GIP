#include <iostream>
#include <string>
using namespace std;

/*unsigned int my_strlen(const char * ptr)
{
	int zaehler = 0;
	for (int i = 0; i >= 0 && i < 20 && ptr != nullptr; i++)
	{
		if ((ptr[i] >= 'a' && ptr[i] <= 'z') || (ptr[i] >= 'A' && ptr[i] <= 'Z') || ptr[i] == ' ')
		{
			zaehler++;
		}
		else if (ptr = nullptr)
		{

		}
	}
	return zaehler;
	
}

int main()
{
	char str_arr[20];
	string eingabe; 
	cout << "Bitte Text eingeben (ggfs. mit Leerzeichen): ? ";
	getline(cin, eingabe);
	for (int i = 0; i >= 0 && i < eingabe.length(); i++)
	{
		str_arr[i] = eingabe.at(i);
	}
	cout << "Ergebnis my_strlen(): " << my_strlen(str_arr) << endl;
	system("PAUSE");
	return 0;
}*/